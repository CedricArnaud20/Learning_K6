name: Run k6 Tests
on: [push]
jobs:
  build: k6 load test
  runs-on: ubuntu-latest
  steps:
    - name: step 1 - checkout
      uses: actions/checkout@v2

    - name: step 2 - run k6 load test
      uses: grafana/k6-actions@v.0.2
      with: 
        file.name: Mao_na_massa/smoke.js 

